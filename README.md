# Détente Exé UI & Backend

Ce dépôt contient :
- Une maquette statique (index.html, styles.css) pour le tableau de bord ERP.
- Un backend Node.js/TypeScript dans `backend/` (Express + Prisma + PostgreSQL).

Consultez `backend/README.md` pour les instructions complètes d’installation, l’architecture et les commandes (dev, build, tests, migrations Prisma).
